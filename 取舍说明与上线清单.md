# 工程取舍说明 & Go-Live 清单

## 一、100 字工程取舍说明

**故意没做**：没接数据库/历史订单——假设当前只能拿到单次聊天上下文；没做多轮对话管理——当前每次请求独立，无法跨会话追踪顾客状态变化；没用向量库/微调——5 段 demo 用 prompt engineering 就够了，过早引入检索或微调只会增加复杂度而无实质收益；没做 UI——导购端界面需要跟企微深度集成，demo 阶段用 CLI 跑通逻辑最快。

## 二、Go-Live 检查清单

| # | 检查项 | 说明 |
|---|--------|------|
| 1 | **数据安全合规** | 聊天数据包含顾客个人信息，需确认数据传输/存储的隐私合规（个人信息保护法） |
| 2 | **模型输出准确性兜底** | AI 可能误判意向或给不恰当建议，需设计「人工审核后发出」机制，不能直接推给顾客 |
| 3 | **与企微/导购工作台集成** | 实际部署需要嵌入导购日常工作流（企微侧边栏/消息提醒），不能靠导购主动打开一个网页 |
| 4 | **导购采纳率监控** | 建立「建议展示 → 导购采纳 → 成交转化」的数据漏斗，否则无法衡量效果 |
| 5 | **延迟与并发** | 门店高峰时段（周末/活动日）多导购同时使用，API 响应需 < 3 秒 |
| 6 | **错别字/口语容错** | 微信聊天充斥错字、语音转文字碎片、表情包，模型需覆盖这些 edge case |

### 🚨 上线当天最可能爆的一项：**#4 导购采纳率监控**

**为什么**：导购是这套系统的最终用户，如果建议质量不稳定（时而精准、时而偏离），导购试用几次就会直接忽略，整个项目等于白做。且导购的反馈是隐性的——他们不会主动说"你的建议不准"，而是直接不用。

**提前怎么堵**：
- 上线首周做 **灰度+人工复核**：只开放给 2-3 个资深导购，每天抽查他们的聊天 + AI 建议，建立「建议质量评分卡」
- 每建议加一个 **「有用 / 没用」** 一键反馈按钮（数据比访谈真实）
- 设定 **采纳率红线**：第 1 周目标 > 60%，低于则回炉调 prompt 或加 few-shot 示例
